    $("div.improve").hide("slow");
    $("div.commentbox").hide("slow");
    $("div.sharebox").hide("slow");
    $('div#questions_<%= @owner_type + '_' + @owner.id.to_s %>').html('<%= escape_javascript(render 'list') %>');
    $('div#questions_<%= @owner_type + '_' + @owner.id.to_s %>').slideToggle();

