$('<%= escape_javascript(render 'notes/new', :paper => @paper)%>').insertAfter('<%= '#folder_list_' + @paper.id.to_s %>');
$('<%= '#folder_list_' + @paper.id.to_s %>').next().toggle('300');
$('<%= '#folder_list_' + @paper.id.to_s %>').click(function(e){
	return false;
});

$('a.dropdown-toggle').click(function(){
	$(this).next().toggle('300');
  });
