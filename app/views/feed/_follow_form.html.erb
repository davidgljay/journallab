<div align = "center">
<%= form_for(Follow.new) do |f| %>
	<%= f.hidden_field :user_id, :value => current_user.id %>
	<%= f.text_field :search_term %>
	<p><%= f.submit "Track PubMed", :class => 'loadingPrompt btn btn-primary' %><%= render '/shared/loading' %></p>
<% end %>
</div>
