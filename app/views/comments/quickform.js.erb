<% if @reload %>
	location.reload();
<% end %>


$('div.quickform').html('Your comment has been posted.');

<% if @owner.class == Figsection %>
	$('<%= @owner.fig.jquery_target %>').find('div.fig_sections').show();
<% end %>

$('<%= @owner.jquery_target %>').addClass('heatBox<%= @heatmap[@owner.inspect][1] %>');	

<% if @form == 'comment' %>

	$('<%= @owner.jquery_target %>').find('form.commentlist').first().find('div.navText').text('<%= pluralize(@comments.count,'Comment') %>');  
	$('<%= @owner.jquery_target %>').find('div.commentbox').first().html('<%= escape_javascript(render '/comments/list') %>'); 
	$('<%= @owner.jquery_target %>').find('div.commentbox').first().show();
	$('<%= @owner.jquery_target %>').find('form.new_comment').first().hide();
<% elsif @form == 'question' %>
	$('<%= @owner.jquery_target %>').find('form.questionlist').first().find('div.navText').text('<%= pluralize(@questions.count,'Question') %>');  
	$('<%= @owner.jquery_target %>').find('div.questionbox').first().html('<%= escape_javascript(render '/questions/list') %>'); 
	$('<%= @owner.jquery_target %>').find('div.questionbox').first().show();
	$('<%= @owner.jquery_target %>').find('form.new_question').first().hide();
<% end %>


$(window).scrollTop($('<%= @owner.jquery_target %>').position().top)
