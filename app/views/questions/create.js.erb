$('<%= @owner.jquery_target %>').find('form.questionlist').first().find('div.navText').text('<%= pluralize(@questions.count,'Questions') %>');  
$('<%= @owner.jquery_target %>').addClass('heatBox<%= @heatmap[@owner.inspect][1] %>');
$('<%= @owner.jquery_target %>').find('div.questionbox').first().html('<%= escape_javascript(render '/questions/list') %>'); 
$('<%= @owner.jquery_target %>').find('form.new_question').first().hide();
