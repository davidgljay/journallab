<%= stylesheet_link_tag 'jl_styles_home', :media => 'screen' %>

<div id="homeLogo"><%= image_tag 'jl_logo_trans_M.png', :id => 'mainlogo' %></div>

<% if signed_in? %>
<div id="homeSearch">
<%= form_tag('/lookup', :method => "get") do  %>
   <h1><%= label_tag :pubmed_id, 'Figure by Figure Discussion of Published Research.' %></h1>
    <table border="0" align="center" cellpadding="0" cellspacing="0">
      <tbody><tr>
        <td>
   	<%= text_field_tag :pubmed_id, '', :class => "inputSearch searchreset" %>
	</td>
<td valign="middle" class = "Search">   <%= submit_tag "Search Pubmed", :id => 'search' %></tb></tr></table>
  

<% end %>
<% end %>
<div id = "homeFeed" class = "feed round">
  <h1><%= @group.name %></h1>
      <%= render '/feed/nav' %>
      <% if !signed_in? %>
	<div id='iccr_feed' class = "homePageFeed round"><%= render '/feed/iccr' %></div>
      <% end %>
      <div id='updates_feed' class = "homePageFeed round hideonload"><%= render '/feed/updates' if signed_in? %></div>
      <div id='most_viewed_feed' class = "homePageFeed round hideonload"></div>
      <% if signed_in? %>
        <% @follows.each do |follow| %>
  	  <div id = <%= follow.classname + "_feed" %> class = "homePageFeed round hideonload"></div>
        <% end %>
	  <div id='follow_form' class = "homePageFeed hideonload"><%= render '/feed/follow_form' %></div>
      <% end %>
</div>
