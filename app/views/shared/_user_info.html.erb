<div class="user_info">

<% if user.anon?(current_user) %>
    <span class="anon_name">
      <%= user.anon_name %>
    </span>
    <% if date %>
    <em><%= render 'shared/display_date', :date => date %></em>
    <% end %>
<% else %>
  <a href="<%= user_path(user) %>">
    <span class="user_name">
      <%= user.name %>
    </span>
  </a>
    <% if date %>
    <em><%= render 'shared/display_date', :date => date %></em>
    <% end %>
<% end %>
</div>
