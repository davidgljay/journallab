<div class="user_info">

<% if user.anon?(current_user) %>
    <span class="anon_name">
      <%= user.anon_name %>
    </span>
    <% if date %>
    	<div class = 'small inline' ><em><%= render 'shared/display_date', :date => date %></em></div>
    <% end %>
<% else %>
  <a href="<%= user_path(user) %>">
    <%= image_tag user.image.thumb('20x20>').url, :alt => item[0].name, :width => "20", :height => "20" %>
    <span class="user_name">
      <%= user.name %>
    </span>
  </a>
    <% if date %>
	    <div class = 'small inline'><em><%= render 'shared/display_date', :date => date %></em></div>
    <% end %>
<% end %>
</div>
