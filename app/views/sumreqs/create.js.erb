$('<%= @item.jquery_target %>').find('div.sumreq_counter').first.html('<%= escape_javascript(pluralize(@item.sumreqs.count, "Summary Request"))  %>');
$('<%= @item.jquery_target %>').find('div.sumreq_button').first.hide();
$('<%= @item.jquery_target %>').find('div.sumreq_confirm').first.show();
