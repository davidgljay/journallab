<div>
  <%= image_tag user.image.thumb('20x20>').url, :alt => user.name, :width => "20", :height => "20" if user.image %>
  <%= link_to user.name, user %> 
	| Signed up on:<%= user.created_at.strftime('%D') %>
	| Papers visited: <%= user.visits.count %> 
	| Actions Taken: <%= user.comments.count + user.assertions.count + user.votes.count + user.questions.count %>
</div>
